#include <stdio.h>
#include "main.h"

/**
 * main - entry point to check
 * @argc: argument count
 * @argv: argument value
 *
 * Return: 0 Always (success)
 */

int main(int argc __attribute__((unused)), char *argv[])
{

	printf("%s\n", argv[0]);
	return (0);
}

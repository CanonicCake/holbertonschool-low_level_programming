#include "main.h"

/**
 * _atoi - convert the strings into pointer
 * @s: pointer to a string
 *
 * Return: s when possible, 0 when no numbers in the string
 */

int _atoi(char *s)
{

	if ()
	{
		
	}
	else
		return(0);
}
